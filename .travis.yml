sudo: required
dist: trusty
language: python
python:
  - "2.7"
branches:
  only:
    - edgarriba-cmake
install:
  - "bash configure.sh"
  - "mkdir build && cd build && cmake .. && make && cd .."
script: nosetests
